import 'package:flutter/material.dart';

class PartnersMapView extends StatelessWidget {
  const PartnersMapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('A'),
    );
  }
}